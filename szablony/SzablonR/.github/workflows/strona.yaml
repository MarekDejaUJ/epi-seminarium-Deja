name: strona

on:
  push:
    branches: [main, master]
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: strona-${{ github.ref }}
  cancel-in-progress: true

jobs:
  strona:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::pkgdown, any::shinylive, local::.
          needs: website

      - name: Dokumentacja pakietu
        run: pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)
        shell: Rscript {0}

      - name: Aplikacja w przeglądarce
        run: Rscript tools/eksport_shinylive.R

      # Katalog docs powstaje w czasie przebiegu i trafia na osobną gałąź.
      # Opcja single-commit sprawia, że gałąź ta ma zawsze jeden commit,
      # więc historia repozytorium nie rośnie o kolejne kopie środowiska R.
      - name: Publikacja
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: docs
          clean: true
          single-commit: true
